name: movies_clean_architecture_mvvm
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  cached_network_image: ^3.2.3
  carousel_slider: ^4.2.1
  dartz: ^0.10.1
  dio: ^5.0.0
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  internet_connection_checker: ^1.0.0+1
  lottie: ^2.2.0
  rxdart: ^0.27.7
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets :
  - assets/json/
